import React from 'react';
import { render } from '@testing-library/react';
import Input from '../components/Input';

describe('The input area', () => {
    test('has a loan size section', () => {

    })

    test('can update the loan size', () => {

    })

    test('has a credit score section', () => {

    })

    test('can update the credit score', () => {

    })

    test('has a property type section', () => {

    })

    test('the property type is a drop down menu', () => {

    })

    test('has a occupancy section', () => {

    })

    test('the occupancy is a drop down menu', () => {

    })
})
